{
    "name": "ownstak",
    "version": "0.1.13",
    "description": "The command line interface for OwnStak platform",
    "type": "module",
    "main": "./src/index.js",
    "homepage": "https://ownstak.com",
    "bin": {
        "ownstak": "./src/commands/index.js",
        "ownstak-cli": "./src/commands/index.js"
    },
    "publishConfig": {
        "directory": "dist",
        "linkDirectory": true
    },
    "scripts": {
        "dev": "chokidar --silent 'src/**/*.ts' --initial 'npm build && npm run link' -c 'clear && npm run build && npm run link'",
        "build": "tsc && cp -r README_NPM.md dist/README.md && npm run build:server && npm run build:serverless && npm run build:copy-templates && npm run build:copy-packagejson",
        "build:server": "esbuild src/compute/server/server.ts --sourcemap --minify --bundle --platform=node --format=esm --outfile=dist/src/compute/server/server.js --log-level=error",
        "build:serverless": "esbuild src/compute/serverless/serverless.ts --sourcemap --minify --bundle --platform=node --format=esm --outfile=dist/src/compute/serverless/serverless.js --log-level=error",
        "build:copy-templates": "cp -r src/templates/* dist/src/templates/",
        "build:copy-packagejson": "node -e \"const fs = require('fs'); const packageJson = require('./package.json'); ['scripts', 'devDependencies', 'jest', 'prettier', 'lint-staged'].forEach(key => delete packageJson[key]); fs.writeFileSync('./dist/package.json', JSON.stringify(packageJson, null, 2));\"",
        "bump-version": "npm version patch --no-git-tag-version",
        "link": "cd dist && npm link > /dev/null",
        "start": "node dist/src/commands/index.js",
        "format": "prettier --write 'src/**/*.ts'",
        "format:check": "prettier --check 'src/**/*.ts'",
        "test": "NODE_OPTIONS=--experimental-vm-modules jest",
        "prepare": "husky || true"
    },
    "dependencies": {
        "@inquirer/prompts": "7.8.1",
        "@vercel/nft": "0.30.0",
        "archiver": "7.0.1",
        "bundle-require": "5.1.0",
        "chalk": "5.5.0",
        "commander": "14.0.0",
        "esbuild": "0.25.8",
        "glob": "11.0.3",
        "mime-types": "3.0.1",
        "semver": "7.7.2"
    },
    "devDependencies": {
        "@types/archiver": "6.0.3",
        "@types/jest": "30.0.0",
        "@types/mime-types": "3.0.1",
        "@types/node": "24.2.0",
        "@types/semver": "7.7.0",
        "chokidar-cli": "3.0.0",
        "husky": "9.1.7",
        "jest": "30.0.5",
        "lint-staged": "16.1.5",
        "prettier": "3.6.2",
        "ts-jest": "29.4.1",
        "typescript": "5.9.2"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "keywords": [
        "ownstak",
        "cli",
        "jamstack"
    ],
    "prettier": {
        "printWidth": 160,
        "tabWidth": 4,
        "useTabs": false,
        "singleQuote": true,
        "trailingComma": "all"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node",
        "extensionsToTreatAsEsm": [
            ".ts"
        ],
        "moduleNameMapper": {
            "^(\\.{1,2}/.*)\\.js$": "$1"
        },
        "transform": {
            "^.+\\.ts?$": [
                "ts-jest",
                {
                    "useESM": true
                }
            ]
        }
    },
    "lint-staged": {
        "*.{js,ts,yml,md,json}": "prettier --write"
    }
}
